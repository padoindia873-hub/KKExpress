// Similar to login.tsx, add fields like name, email. For now, placeholder.
import React from 'react';
import { View, Text } from 'react-native';

export default function Signup() {
  return (
    <View>
      <Text>Signup Screen (Implement similar to Login)</Text>
    </View>
  );
}